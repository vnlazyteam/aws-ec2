[Unit]
Description=Consul Startup process
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash -c '{{ consul.binary_path }}/consul agent -config-dir {{ consul.config_dir }}'
TimeoutStartSec=0

[Install]
WantedBy=default.target
